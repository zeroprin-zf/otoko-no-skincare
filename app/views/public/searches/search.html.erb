<div class="container">
  <h4 class="p-4 font-weight-bold">検索結果</h4>
  <div class="row">
    <div class="col-2">
     <%= render 'public/categories/category', categories: @categories %>
    </div>
    <% if @results.present? %>
     <%= render 'template/index', items: @results %>
    <% else %>
  <p>検索結果がありません</p>
 <% end %>
 </div>
</div>
<div class="pagination justify-content-center">
  <%= paginate @results %><!--変わるかも-->
</div>

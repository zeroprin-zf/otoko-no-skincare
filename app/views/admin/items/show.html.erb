<div class="container">
  <h4 class="p-4 font-weight-bold">投稿詳細</h4>
  <div class="row">
  <div class="text-center col-md-3">
    <%= image_tag @item.get_image,size: "200x200" %>
  </div>
   <div class="offset-md-1 col-md-5 text-center">
    <p>ユーザー名: <%= @item.user.name %></p>
    <p>投稿商品名: <%= @item.name %></p>
    <p>価格: <%= @item.value %>円</p>
    <p>カテゴリー名: <%= @item.category.name %></p>
    <p>感想: <%= @item.review %></p>
    <p>コメント件数: <%= @item.comments.count %></p>
    <p><%= link_to "コメント一覧",  admin_item_comments_path(@item.id) %></p>
    <p><%= link_to "投稿を削除", admin_item_path(@item.id),class: "btn btn-danger" ,method: :delete, "data-confirm" => "本当に削除しますか？" %></p>
     <!--adminの側だけで消えるかもしれない-->
   </div>
  </div>
 </div>
</div>